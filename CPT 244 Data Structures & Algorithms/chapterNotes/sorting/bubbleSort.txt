bubbleSort(x,n)
{
	topIndex = n=1;												// last index in array
	
	while (topIndex > 1;)										// not at the front
	{
		for (int j = 0; j < topIndex;)
			{
				if (x[j] > x[j+1])
				{
					// swap the two variables
					int temp = 0;
					temp 	= x[j];
					x[j] 	= x[j+1];
					x[j+1] 	= temp;
				}
				topIndex = 
			}
	}
}