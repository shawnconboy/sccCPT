#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main()
{
    system("clear");

    // // 5 to the 2nd power
    // cout << pow(5.0, 2) << endl;

    // // 2 to the 4th power
    // cout << pow(2, 4) << endl;

    cout << sqrt(25) << endl;
    cout << sqrt(400) << endl;

    return 0;
}